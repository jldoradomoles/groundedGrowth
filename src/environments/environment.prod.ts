// Configuración de producción para Angular
export const environment = {
  production: true,
  apiUrl: '/api', // En producción usará el mismo dominio
  appName: 'GroundedGrowth',
  version: '1.0.0',
};
